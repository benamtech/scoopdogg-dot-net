logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.vercel
.env*

# Admin screens captured by gates/admin-browser.mjs. They show real customer names,
# emails and phone numbers straight out of the leads table. Evidence for a run, never
# something to commit.
output/admin-screens/

# Astro's generated types and content cache.
.astro/

# Installed agent skills, and the symlinks into them. `skills-lock.json` is the record
# worth committing; these are the node_modules of it, and the symlinks break on clone.
.agents/
.claude/

# AMTECH planning. NEVER commit: this repo is PUBLIC.
# These are our documents, not the client's deliverable. Backed up by
# amtech-biz-brain/bin/snapshot.py via INCLUDE_CLIENT_PATHS, not by git.
client-portal-and-ai-employee-plans/

# gate scratch build (gates/_compile.mjs)
.gate-build

# migration rehearsal receipts (scripts/rehearse-migration.mjs)
output/rehearsals/

# local artifacts: lighthouse reports, demo receipts, gate failure screenshots
output/

# The dev server's own compiled output (see gates/_compile.mjs on why it is not .gate-build)
.dev-build/
